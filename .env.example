# URL dasar API SiLAKES (routes/silacare.php), termasuk /api di akhir.
NUXT_PUBLIC_API_BASE=https://api.silakes.labkesdasumenep.id/api

# API key Gemini untuk OCR KTP client-side (sama seperti FE_SiLAKES) --
# JANGAN taruh key dengan quota/billing sensitif, key ini akan terlihat oleh
# browser (client-side call), sama seperti pola yang sudah dipakai FE_SiLAKES.
VITE_GEMINI_API_KEY=
